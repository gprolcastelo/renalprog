# Minimal requirements for running tests in CI/CD
# This file is used to reduce disk space usage in GitHub Actions

# Testing framework
pytest>=7.3.0
pytest-cov>=4.1.0

# Core scientific computing (CPU-only versions when possible)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<2.2.0
scikit-learn>=1.3.0,<1.4.0
scipy>=1.10.0,<1.12.0

# Visualization (minimal)
matplotlib>=3.7.0,<3.8.0
seaborn>=0.12.0,<0.14.0

# Utilities
tqdm>=4.65.0
h5py>=3.8.0
requests>=2.31.0
pyyaml>=6.0
colorlog>=6.7.0

# Bioinformatics
pydeseq2>=0.4.0
statsmodels>=0.14.0

# Graph analysis
networkx>=3.0
graphviz>=0.20.0

# Note: Install PyTorch separately with CPU-only version:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

